{
  "name": "krc-static-server",
  "version": "1.0.0",
  "private": true,
  "description": "Dev server to serve the KRC Global Map application without external deps.",
  "scripts": {
    "start": "node scripts/dev-server.js",
    "start:port": "PORT=8000 node scripts/dev-server.js"
  }
}

